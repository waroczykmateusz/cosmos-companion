name: cosmic_companion
description: Cosmic Companion — astrofotografia i astrologia, offline-first.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.7.0

dependencies:
  crypto: ^3.0.6
  cupertino_icons: ^1.0.8
  drift: ^2.22.1
  drift_flutter: ^0.2.4
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_map: ^7.0.0
  flutter_riverpod: ^2.6.1
  flutter_secure_storage: ^9.2.2
  freezed_annotation: ^2.4.4
  geolocator: ^13.0.2
  go_router: ^15.1.2
  intl: ^0.20.2
  json_annotation: ^4.9.0
  latlong2: ^0.9.0
  local_auth: ^2.3.0
  permission_handler: ^11.3.1
  sentry_flutter: ^8.14.2
  sweph: ^2.10.0
  uuid: ^4.5.1
  # astronomy_engine: TBD — git dependency, nazwa paczki do ustalenia

dev_dependencies:
  build_runner: ^2.4.14
  drift_dev: ^2.22.1
  flutter_test:
    sdk: flutter
  freezed: ^2.5.7
  integration_test:
    sdk: flutter
  json_serializable: ^6.9.0
  mocktail: ^1.0.4
  very_good_analysis: ^7.0.0
  # arcore_flutter_plugin: ^0.x        # AR Sky Map (Android)
  # arkit_plugin: ^1.x                 # AR Sky Map (iOS)
  # maplibre_gl: ^0.x                  # Light Pollution Map
  # share_plus: ^9.x                   # Stellarium/SkySafari export
  # supabase_flutter: ^2.x             # Cloud sync

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/ephemeris/
    - assets/icons/zodiac/
